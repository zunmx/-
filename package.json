{
  "name": "dian_ming_qi",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package64": "electron-packager . demoName --platform=win32  --arch=x64  --icon=favicon.ico --out=./out --asar --app-version=0.0.2 --overwrite --ignore=node_modules",
    "package32": "electron-packager . demoName --platform=win32  --arch=ia32 --icon=favicon.ico --out=./out --asar --app-version=0.0.2 --overwrite --ignore=node_modules"
  },
  "author": "zunmx",
  "license": "ISC",
  "devDependencies": {
    "electron": "^20.1.4",
    "electron-packager": "^16.0.0"
  }
}
